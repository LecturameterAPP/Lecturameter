<?xml version="1.0" encoding="utf-8"?>
<!--
    Android Auto Backup rules (API 23-30).
    Includes all SharedPreferences (libros, sesiones, wrapped_history, dark_mode).
    Excludes the cache dir (temp CSV/JSON exports).
-->
<full-backup-content>
    <!-- A6: SOLO se respalda lecturameter.xml. El trial y el lockout del canje viven en
         lecturameter_pro_local.xml, que al no estar incluido aquí NUNCA se respalda
         (reinstalar no reinicia ni reactiva la prueba gratis). Lint prohíbe un exclude
         explícito de un path no incluido, por eso se documenta en vez de excluirse. -->
    <include domain="sharedpref" path="lecturameter.xml" />
</full-backup-content>
