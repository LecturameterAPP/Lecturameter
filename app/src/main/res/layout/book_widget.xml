<?xml version="1.0" encoding="utf-8"?>
<!-- v21.35: barra de progreso sky + emoji 📊 en % -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="10dp"
    android:paddingTop="10dp"
    android:paddingEnd="10dp"
    android:paddingBottom="10dp">

    <FrameLayout
        android:id="@+id/widget_cover_frame"
        android:layout_width="64dp"
        android:layout_height="92dp"
        android:background="@drawable/widget_cover_bg">

        <ImageView
            android:id="@+id/widget_cover"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/app_name"
            android:scaleType="centerCrop"
            android:visibility="gone" />

        <TextView
            android:id="@+id/widget_cover_placeholder"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center"
            android:text="📖"
            android:textColor="#F0F0F0"
            android:textSize="26sp" />
    </FrameLayout>

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_marginStart="12dp"
        android:gravity="center_vertical"
        android:orientation="vertical"
        android:layout_weight="1">

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="2"
            android:textColor="#F0F0F0"
            android:textSize="15sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/widget_author"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="#8899AA"
            android:textSize="11sp" />

        <LinearLayout
            android:id="@+id/widget_chips_row"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:id="@+id/widget_days_chip"
                style="@style/WidgetChipText"
                android:layout_width="wrap_content"
                android:layout_marginEnd="2dp" />

            <TextView
                android:id="@+id/widget_minutes_chip"
                style="@style/WidgetChipText"
                android:layout_width="wrap_content"
                android:layout_marginEnd="2dp"
                android:visibility="gone" />

            <TextView
                android:id="@+id/widget_sessions_chip"
                style="@style/WidgetChipText"
                android:layout_width="wrap_content"
                android:layout_marginEnd="2dp" />

            <TextView
                android:id="@+id/widget_pages_chip"
                style="@style/WidgetChipText"
                android:layout_width="wrap_content"
                android:layout_marginEnd="2dp"
                android:visibility="gone" />

            <TextView
                android:id="@+id/widget_percent_chip"
                style="@style/WidgetChipText"
                android:layout_width="wrap_content"
                android:visibility="gone" />
        </LinearLayout>

        <!-- v21.35: ProgressBar sky entre chips y fecha -->
        <ProgressBar
            android:id="@+id/widget_progress_bar"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="4dp"
            android:layout_marginTop="5dp"
            android:max="100"
            android:progress="0"
            android:progressTint="#0EA5E9"
            android:progressBackgroundTint="#40FFFFFF"
            android:visibility="gone" />

        <TextView
            android:id="@+id/widget_updated"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="#8899AA"
            android:textSize="10sp" />
    </LinearLayout>
</LinearLayout>
