<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema transparente para el widget: el sistema no pinta ningún fondo
         sobre las celdas del launcher, así solo se ve la tarjeta redondeada -->
    <style name="Widget.Lecturameter.Transparent" parent="android:Theme">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="WidgetChipText">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/widget_chip_bg</item>
        <item name="android:ellipsize">end</item>
        <item name="android:maxLines">1</item>
        <item name="android:paddingStart">4dp</item>
        <item name="android:paddingTop">3dp</item>
        <item name="android:paddingEnd">4dp</item>
        <item name="android:paddingBottom">3dp</item>
        <item name="android:textColor">#F0F0F0</item>
        <item name="android:textSize">10sp</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>
