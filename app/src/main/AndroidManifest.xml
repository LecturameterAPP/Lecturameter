<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- USE_FULL_SCREEN_INTENT: omitido intencionadamente.
         Desde Android 14 solo se concede a apps de llamadas/alarmas; Play Console
         exige justificación. Sin el permiso, setFullScreenIntent() degrada a
         heads-up notification, que es suficiente para avisar de fin de sesión. -->
    <!-- <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" /> -->
    <!-- specialUse: el cronómetro de lectura no reproduce audio/vídeo, así que
         mediaPlayback no aplica y Play Console lo rechazaría con targetSdk 35.
         specialUse requiere justificación en la ficha de Play Console:
         "Foreground timer that tracks active reading sessions; the user starts
         and stops it explicitly; it must survive backgrounding to keep an
         accurate elapsed-time count." -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".LecturameterApplication"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@android:style/Theme.Material.NoActionBar"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false">
        <!-- Icono conmutable (Pro): el filtro LAUNCHER vive en DOS activity-alias
             (clásico activo por defecto, dorado apagado) y NUNCA en la activity real.
             Así cambiar de icono solo alterna aliases y jamás deshabilita .MainActivity,
             que es el destino explícito de los PendingIntents del widget y de las
             notificaciones (deshabilitarla los rompería). Coste asumido: al actualizar
             desde la 2.7 el acceso directo anclado se recoloca una vez. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|keyboard|navigation">
            <!-- Deep link desde el widget al detalle de un libro.
                 El widget construye intents lecturameter://book/{id} que se preservan
                 en el PendingIntent que crea Glance (los extras se pierden, la URI no). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="lecturameter" android:host="book"/>
            </intent-filter>
            <!-- Deep link desde las notificaciones de resúmenes:
                 lecturameter://recap/{weekly|wrapped/{año}} -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="lecturameter" android:host="recap"/>
            </intent-filter>
            <!-- Deep link del candado del widget Pro a la hoja de venta:
                 lecturameter://pro -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:scheme="lecturameter" android:host="pro"/>
            </intent-filter>
        </activity>
        <activity-alias
            android:name=".LauncherClassic"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:enabled="true"
            android:icon="@mipmap/ic_launcher"
            android:roundIcon="@mipmap/ic_launcher_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity-alias>
        <activity-alias
            android:name=".LauncherGold"
            android:targetActivity=".MainActivity"
            android:exported="true"
            android:enabled="false"
            android:icon="@mipmap/ic_launcher_pro"
            android:roundIcon="@mipmap/ic_launcher_pro_round">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity-alias>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- ── Book Widget ─────────────────────────────────────────────────── -->
        <receiver
            android:name=".widget.BookWidgetReceiver"
            android:exported="true"
            android:label="Lecturameter Widget">
            <!-- Auditoría seguridad 13-07: com.lecturameter.REFRESH_WIDGET fuera del
                 filter — el receiver es exported (requisito de APPWIDGET_UPDATE) y la
                 acción propia permitía a CUALQUIER app disparar refrescos del widget.
                 Nada la enviaba: el refresco interno usa requestBookWidgetUpdate(). -->
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/book_widget_info" />
        </receiver>

        <!-- ── Stats Widget Pro ────────────────────────────────────────── -->
        <receiver
            android:name=".widget.StatsWidgetReceiver"
            android:exported="true"
            android:label="@string/stats_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/stats_widget_info" />
        </receiver>

        <!-- Widget configuration activity (book picker) -->
        <activity
            android:name=".widget.WidgetConfigActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@android:style/Theme.Material.NoActionBar">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <service
            android:name=".TimerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <!-- Play Console justification for specialUse:
                 Reading-session chronometer that must keep running while
                 the app is backgrounded so elapsed time stays accurate. -->
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="reading_timer" />
        </service>

        <!-- ── ISBN Scanner ───────────────────────────────────────────────────── -->
        <activity
            android:name=".ScannerActivity"
            android:exported="false"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Material.NoActionBar" />

    </application>
</manifest>
